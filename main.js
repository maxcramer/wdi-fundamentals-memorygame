var cardOne = [{
	rank: 'queen',
	suit: 'hearts',
	cardImage: 'images/queen-of-hearts.png'
}];
var cardTwo = [{
	rank: 'queen',
	suit: 'diamonds',
	cardImage: 'images/queen-of-diamonds.png'
}];
var cardThree = [{
	rank: 'king',
	suit: 'hearts',
	cardImage: 'images/king-of-hearts.png'
}];
var cardFour = [{
	rank: 'king',
	suit: 'diamonds',
	cardImage: 'images/king-of-diamonds.png'
}];

console.log("User flipped " + cardOne);
console.log("User flipped " + cardTwo);
var cardsInPlay.push["cardOne", "cardTwo"];
function flipCard () {
	var cardId = this.getAttribute('data-id');

this.setAttribute('src').cards[cardId].cardimage;

if (length) {
	cardsInPlay === [2]window.alert("You found a match");
} else {
	cardsInPlay[0]window.alert("Sorry, try again");
};
console.log("User flipped" + cards[cardId].rank);
};
function createBoard 
for (var i = 0; i < cardOne.length; i++) {
}
for (var i = 0; i < cardTwo.length; i++) {
}
for (var i = 0; i < cardThree.length; i++) {
}
for (var i = 0; i < cardFour.length; i++) {

var cardElement = document.createElement('img');

cardElement.setAttribute('src','images/back.png');

cardElement.setAttribute[i]('data-id');

addEventListener(click).cardElement(flipCard);

cardElement.appendChild(game-board);

};

cardsInPlay.push(cards[cardId].rank);
console.log('cardImage');
console.log('suit');
function checkForMatch();
return 0, 2;
function checkForMatch{if (cardsInPlay[0] === cardsInPlay[1]) {
console.log("You found a match!");
} else {
console.log("Sorry, try again.");
};
}; 

function createBoard ();
